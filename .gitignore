# ===== TiandituTools — repository hygiene =====
# Debugging / diagnostic scratch from dev sessions (NOT project source).
# Every file here starts with "_" and is disposable.
_*/
_*
._*

# Python bytecode / caches
__pycache__/
*.pyc

# WorkBuddy internal memory (not for distribution)
.workbuddy/

# OS cruft
.DS_Store
Thumbs.db

# Superseded build artifact (current build is TiandituTools.esriaddin)
tianditu-tools-0.6.0.zip
